# ReturnKits Customer Portal

Multi-tenant portal for a UK IT asset recovery / reverse logistics business. Customers order kits to return or redeploy hardware; ReturnKits fulfils and tracks them.

**Status:** greenfield. No code yet. Rebuilding off a Base44 prototype (schema validated, no real data — see `docs/returnkits-base44-audit.md`).

## Documents — read before building

| Doc | What it is |
|---|---|
| `docs/returnkits-portal-architecture.md` | The design. Every decision, with reasoning. Sections referenced as §N. |
| `docs/returnkits-implementation-plan.md` | Phases 0–14 with exit criteria. **Follow this order.** |
| `docs/returnkits-getting-started.md` | Stack, tooling, setup commands. |
| `docs/returnkits-base44-audit.md` | What went wrong in the prototype. Explains several design choices. |

When a decision seems arbitrary, it's in the architecture doc with reasoning. Check before changing it.

## Build approach

| Layer | Built with |
|---|---|
| Database, RLS, migrations, tests | **Hand-written SQL** — never generated |
| Money & concurrency (webhooks, credits, references) | **Hand-written** — never generated |
| Customer portal UI | **Lovable** (real React/TS, two-way GitHub sync) |
| Ops dashboard | **Retool** over the same Postgres |

**The governing rule: you own the schema. Lovable builds on top of it. The RLS suite is the gate.**

Create tables and policies first, then point Lovable at the existing Supabase project. If Lovable designs the schema it will produce permissive RLS that looks correct but fails cross-tenant rigour — exactly the bug the Base44 audit found. Run the RLS suite after anything Lovable changes.

## Stack

TypeScript · Next.js 15 (App Router) · Supabase (Postgres + Auth + Storage + Edge Functions, **London region**) · Lovable · Retool · Stripe · Sendcloud · Resend + React Email · Tailwind + shadcn/ui · Zod · Vitest · Sentry

**Single app, not a monorepo.** Business logic lives in `lib/`. There is no `apps/ops` — that's Retool.

## Non-negotiable rules

1. **RLS test suite must be green.** Never ship with it red. Tests assert *both* directions: company A can't read company B's data, **and** user 2 in company A *can* read user 1's orders. The prototype silently failed the second one for months.
2. **`service_role` key never reaches the client.** Not in the portal bundle, not in `NEXT_PUBLIC_*`. Retool holds its own privileged connection server-side.
3. **All permission checks go through `can(user, action, resource)`** in `lib/`. Never inline `if (user.role === 'admin')`.
4. **Every inbound webhook is signature-verified and idempotent**, keyed on the provider's event ID. Stripe, Sendcloud, Resend, BambooHR. **Hand-written, never generated.**
5. **Money is integer pence. Prices stored ex-VAT only**, inc-VAT computed at display. The prototype stored both and they drifted.
6. **New tenant-scoped table** → RLS policy + both test directions land in the same commit.
7. **Retool writes call the app's API**, never straight to tables. Reads may hit Postgres directly. Retool bypasses RLS, so audit logging happens in the API or via DB triggers.
8. **Dates in Europe/London.** Order references, working-day calculations, and dispatch windows all derive from business-local dates, not UTC.
9. **Review every migration Lovable proposes** before applying it.

## Locked decisions (don't re-litigate)

- **Flat order model** — one `orders` row per kit, tracking columns on the row, `bundle_id` groups multi-item orders. No separate `kits`/`shipment_legs` tables.
- **No guest orders.** `company_id` is `NOT NULL` everywhere. One tenant path per RLS policy.
- **Invite-only company joining.** Signed single-use expiring tokens are the sole access grant. `companies.domain` is descriptive metadata and must **never** be used for authorization.
- **Self-serve signup** — anyone can register a company. No card required at signup.
- **Prices ex-VAT**: Laptop £65, Phone £40, Monitor £85. Tablet and Accessories exist but inactive. VAT shown as a separate line at cart, checkout, and invoice.
- **Enhanced Cover** (never call it "insurance" — FCA): £5/£10/£20 ex-VAT for £500/£1,000/£2,000, carrier declared-value passthrough, separate invoice line, 20% VAT. **Built out of order, 20260812** — originally Phase 10/deferred, but enterprise pricing had already been quoted to a prospect assuming it existed, so it was pulled forward. `cover_tiers` (id/label/max_value_pence/price_ex_vat_pence/vat_rate/active) — same world-readable-but-read-only RLS shape as `kit_types`, no write policy, managed by migration only. `orders.cover_tier_id` + `orders.cover_price_ex_vat_pence` snapshotted at order creation via `create_order`/`create_internal_order`'s optional `p_cover_tier_id` param, same immutable-snapshot pattern as kit price. `create-checkout-session` adds cover as its own Stripe line item per order, never folded into the kit line, VAT computed from `cover_tiers.vat_rate`. **Declared value still has to be entered manually in Sendcloud when staff buy the label** — Phase 6 never built `buyLabel()` automation, so there's no API path to pass it through; the Retool order panel shows the cover tier clearly for exactly this reason. Claims are deliberately minimal: `flag_cover_claim(p_order_id, p_actor_id, p_notes default null)` (staff-only, same `service_role` + `assert_internal_actor` pattern as `cancel_order`/`mark_order_paid`) just records that a claim was filed — actually pursuing it with the carrier is still a phone call/email a human makes, not something the app does.
- **Credits typed per kit type.** Laptop credits can't buy phone kits. Append-only ledger; balance is the `SUM`. **Built 20260812 (prepaid credits only — pulled forward same day as Enhanced Cover, in response to "make it easier to order without paying constantly").** Scope locked with the user: prepaid credits only, no free-kit promo (§22 stays unbuilt — that's the part of the design that actually needs the "was this claimed atomically" bug class this project's audit flagged; nobody's asked to prepay yet outside this exact request). Same per-unit price as self-serve `kit_types` — no bulk/volume discount logic. `credit_ledger` (id/company_id/kit_type_id/transaction_type[`purchase`|`redemption`|`adjustment`]/direction[`credit`|`debit`]/quantity/balance_after/order_id/invoice_id/stripe_checkout_session_id/reason/actor_id/created_at), same RLS shape as `invoices` (own company + internal_ops), no insert/update/delete policy for authenticated — every write goes through a RPC or the webhook. `orders.paid_with_credit` + `orders.credit_transaction_id` snapshot which redemption (if any) financed the order; a `orders_credit_snapshot_consistent` CHECK keeps the two in lockstep. **v1 restriction:** a credit-paid order can't also carry Enhanced Cover (`orders_credit_excludes_cover` CHECK) — credit only ever covers the kit, and "partly paid by credit, partly owed by card for cover" was exactly the kind of complexity this scope decision was meant to avoid. `create_order`/`create_internal_order` take an optional `p_pay_with_credit` param: checks the balance inside the same transaction as the debit insert (concurrent redemptions for the last credit serialize, never double-spend — same atomicity discipline as `next_reference_number`), creates the order already `payment_status = 'paid'`. Buying credits is always by card — `create-credit-checkout-session` (Edge Function) creates a Stripe Checkout Session, `record_credit_purchase` (webhook-only RPC, same `stripe_webhook_events`-keyed idempotency pattern as `record_stripe_payment`) issues a real gapless invoice number and credits the ledger. **Saved card is for topping up only, never for paying an individual order directly** — `create-card-setup-session` creates a Stripe Checkout Session in `mode:'setup'` (no charge), `record_card_setup` (webhook-only) caches the resulting PaymentMethod id on `companies.stripe_payment_method_id` and sets it as the Stripe customer's default payment method so the next credit purchase offers it as a one-click option. `stripe-webhook` now branches on `metadata.type` (`order_payment` — the default/legacy case for sessions with no `type` at all — `credit_purchase`, or `card_setup`) to route to the right RPC. `cancel_order` restores 1 credit (an `adjustment` ledger row, not a mutation of the original debit) when the cancelled order was `paid_with_credit` — consistent with the existing "purchased credits are restored" rule below, since every credit in this v1 is a purchased one.
- **No refunds — and never automated.** Cancellation is an admin-only state (`cancel_order`). Purchased credits are restored; **promo credits are forfeited**. When a genuine refund is owed (e.g. a paid-by-card order cancelled before dispatch), that's a **manual action staff take directly in the Stripe dashboard** — `cancel_order` only ever updates our own tables and never calls the Stripe API, by design. Confirmed with the user 20260813: refunds will always be issued manually, no `create_refund`/Stripe refund API integration is planned.
- **Order references**: `RKL|RKT|RKP|RKM|RKA-YYMMDD-NNN`, bundles `BND-YYMMDD-NNN`. Atomic generation via `reference_counters`, immutable once issued, rolls to 4 digits past 999. **Never parse by fixed width** — split on `-`.
- **Invoice numbers** are a *separate*, strictly gapless Postgres sequence (UK VAT requirement). Cancelled invoices are voided, never deleted.
- **SLA**: next-working-day dispatch, working days exclude UK bank holidays.
- **Shipping**: Sendcloud, behind a `ShippingProvider` interface. Tracking statuses normalised to our own enum. Provider is expected to change.
- **`fulfilment_status` has an `in_transit` state (added 20260811, Phase 6 tracking).** Set automatically by `apply_sendcloud_tracking_event()` off real Sendcloud parcel-status-changed webhooks — never by a staff RPC. Both legs use the same status name, whichever leg (`outbound_tracking_number` or `return_tracking_number`) is currently active on the order. `mark_return_completed` and `confirm_received` both accept `'dispatched'` or `'in_transit'` as their pre-state, so a real tracking scan landing before close-out never blocks staff/customer from completing the order manually.
- **Sendcloud "delivered" auto-completes the order (added 20260813).** Same `apply_sendcloud_tracking_event()` function, a second guarded branch: a mapped `delivered` status_code moves `fulfilment_status` from `'dispatched'` or `'in_transit'` straight to `'completed'` — the exact terminal state `confirm_received`/`mark_return_completed` already produce manually, not the separate unused `'delivered'` enum value. Clarified with the user 20260813: return orders ship back to the *ordering company's own* HQ/IT address (`orders.return_address_id`, their own `addresses` row) — never a ReturnKits warehouse — so a courier "delivered" scan is exactly as legitimate a completion signal on the return leg as it is on the outbound leg of a ship-to-new-employee order; both service types now auto-complete symmetrically off the same event. `ship_to_new_employee` orders additionally get `confirmed_received_at` stamped with the event timestamp (`confirmed_received_by` stays null — system-triggered, not a specific user's click) so that column means the same thing regardless of whether a human clicked "Confirm received" or Sendcloud reported it first. This directly closes the gap the implementation plan flagged: "the real fix is Phase 6's Sendcloud tracking webhooks updating `fulfilment_status` automatically instead of relying on manual confirmation from someone without visibility." Investigated (not copied) Base44's old `pollSendcloudTracking` function for domain knowledge before building this — confirmed the real Sendcloud v2 tracking-poll endpoint and credential shape, but its observed status vocabulary (`announced`, `shipment-on-route`, `delivered`, from the poll endpoint's `parent_status` field) is from a different API surface than our webhook's `status_code` field, so it was deliberately **not** merged into `sendcloud_status_map` without independent confirmation against a real webhook payload — `sendcloud_status_map` stays exactly as seeded, extend only from observed traffic per its existing note.
- **Phase 6 is tracking-only, not label automation.** Labels stay manual in Sendcloud's dashboard (decision 20260811: "I will do labels manually but we need tracking in the portal") — no `buyLabel`, rate shopping, or address validation was built. Sendcloud webhooks are scoped to the *integration* a parcel belongs to, not to how the label was created, so a manually-created label still fires `parcel-status-changed` events; the "return parcels only get webhooks if the outgoing shipment was created through the API Shop" caveat in Sendcloud's docs is read as referring to their own RMA/returns-portal product, which ReturnKits doesn't use — worth confirming empirically against the first live return order. `sendcloud-webhook` (Edge Function) verifies the HMAC-SHA256 `Sendcloud-Signature` header (hex-encoded, not base64) and calls `apply_sendcloud_tracking_event()`, which matches by tracking number and looks up `sendcloud_status_map` to normalise Sendcloud's `status_code` vocabulary. That map is a best-effort seed, not a verified full list (Sendcloud's live status API wasn't reachable to enumerate it) — extend via plain SQL insert as real payloads are observed, no redeploy needed. Unmapped codes are logged and ignored, never guessed into a wrong transition.
- **Employees never log in.** `employees` is a recipient directory (name, email, address, last-kit-ordered) — not a portal user, no auth identity, no RLS-scoped access of their own. The leaver/joiner just receives a physical kit. Every order action (creation, `confirm_received`) is performed by the company's portal user who placed the order, and every *order-detail* email (confirmation, and the full dispatched/check-in emails with pricing/reference/tracking) goes to that user (`orders.created_by` → `users.email`) — never to the employee's address on file, and never with pricing. (Employees do now get their own separate, minimal *passive* notices — see the next bullet — but those are one-way informational emails, not portal access, and carry no order/pricing detail.)
- **Employee-facing passive notifications (added 20260813).** The employee named on an order (`orders.employee_id` → `employees.email`) now also gets a copy of exactly two events — `dispatched` ("a ReturnKits box is on its way to you") and `checkin_sent` (the return-leg "please post it back" reminder) — sent alongside, not instead of, the existing customer-facing email to `orders.created_by`. Explicit scope, confirmed with the user: no pricing, no order reference, no company billing detail ever shown to the employee; nothing to click, purely a nudge; not extended to `order_confirmation` (nothing physical has moved yet) or `checkin_received` (the ship-to-new-employee "has it arrived?" question is addressed to whoever can check a portal, not the recipient). Nudges stop automatically once the device is on its way back — no new logic for this: `checkin_sent` is only ever sent to orders still sitting in `fulfilment_status = 'dispatched'` (`orders_needing_checkin()`'s existing filter), so the moment a return tracking scan flips the order to `in_transit` or `completed`, the order drops out of eligibility and the employee stops hearing about it, exactly like the existing customer-facing nudge. Schema: `communication_log.audience` CHECK widened to add `'employee'` alongside `'customer'`/`'internal'` (migration `20260813120000_employee_facing_notifications.sql`) — no RLS policy change needed, `communication_log_select_own_company` already covers it (proven, not assumed, in `tests/rls.test.ts`). Rendering/sending lives in `send-order-email` (same Edge Function, same auth model, same suppression-list gate, same `notification_preferences` toggle as the customer-facing send it piggybacks on — muting `dispatched` for a company mutes both copies with one switch). One-shot idempotency for the `dispatched` employee copy is scoped to the single order, not bundle-aware like the customer confirmation — an employee only cares about their own kit. Silently no-ops if the employee has no email on file. **Edge Function deploy note:** `send-order-email` inlines its own Sentry error reporter (previously imported from `supabase/functions/_shared/sentry.ts`) because the `deploy_edge_function` MCP tool wasn't reliably bundling that cross-function relative import — repeated deploys failed with `Module not found` even with the shared file included in the payload. If other functions hit the same issue, worth revisiting whether the shared-module pattern needs the Supabase CLI instead of MCP-based deploys.
- **Per-order `notify_employee` opt-in (added 20260813, same day as the feature above).** The employee-facing nudges above turned out to need a control: **off by default**, one flag covers both `dispatched` and `checkin_sent` together (not two separate toggles — scoped with the user as a single "notify this employee about this order, yes/no" decision), set once at order creation via `create_order`/`create_internal_order`'s optional `p_notify_employee` param (`orders.notify_employee boolean not null default false`, migration `20260813130000_notify_employee_order_flag.sql`), no dedicated edit RPC — same immutable-snapshot pattern as kit price/cover tier/credit redemption. `send-order-email`'s `sendEmployeeCopy()` now checks this flag first, before the employee-has-no-email check and independent of (on top of, not instead of) the company-level `notification_preferences` gate — all three must clear for a send. Lovable's New Order form (`orders.new.tsx`) has a checkbox in the Employee card, unchecked by default, labelled "Also notify the employee by email"; `create_internal_order` accepts the same param for symmetry but no Retool UI toggle was built for it yet (Retool internal orders are staff placing orders on a customer's behalf, a different flow from "the orderer" the request was actually about — add a Retool toggle later if wanted).
- **Manual, one-off employee entry on orders (added 20260813).** The orderer can type a recipient's name/email/address directly into New Order instead of picking from the `employees` directory — explicitly confirmed with the user this must NOT create a directory row: "i want them to be able to add an address manually without saving the record." `orders.employee_id` is now nullable; seven new nullable snapshot columns (`employee_name`, `employee_email`, `employee_address_line1/2`, `employee_city`, `employee_postcode`, `employee_country`) hold the same shape of data directly on the order when this path is used — deliberately not reusing `employees.full_name` etc. as column names so it's unambiguous at a glance that these are order-level snapshots, not a join (migration `20260813140000_manual_employee_entry_on_order.sql`). `orders_employee_source_check` CHECK enforces exactly one of `employee_id`/`employee_name` is ever set — never both, never neither. `create_order`/`create_internal_order` both gained matching optional `p_employee_name`/`p_employee_email`/`p_employee_address_line1/2`/`p_employee_city`/`p_employee_postcode`/`p_employee_country` params: reject both an id and a name, reject neither, reject a blank name, default `country` to `'GB'` when the manual path is used and none was given. No DB-level requirement that a manual entry include an address — mirrors the existing `employee_id` path, where `employees.address_line1` has never been enforced either, deliberately not introducing asymmetric strictness between the two paths. Downstream: `send-order-email` and `generate-print-pack` both gained a `resolveEmployee()`-style helper that reads name/email/address from EITHER the `employees` join OR the snapshot columns, so every email template and the Print Pack shipping label render identically regardless of which path was used; `generate-print-pack` also had its Sentry reporter inlined (same `Module not found` MCP-deploy issue as `send-order-email`, see the note above). Lovable's New Order form (`orders.new.tsx`) gained a third Employee-card mode alongside the existing "pick existing" and "add to directory permanently" ones: "Enter details manually instead," a form (name required, email/address all optional) that is never written to `public.employees`, only passed straight through as RPC params. **Known gap, not addressed:** Retool's order detail panel likely still reads employee info via the `employee_id` join only, so a manually-entered order's recipient may show blank there — the panel wasn't touched as part of this change; worth a follow-up if staff need to see manually-entered recipient details in Retool.
- **`mark_order_dispatched` refuses to dispatch an unpaid order (added 20260811).** `payment_status` must be `'paid'`, no exceptions — but `payment_status` is only otherwise set to `'paid'` by `record_stripe_payment` (the Stripe webhook), and orders created manually in Retool via `create_internal_order` (`source = 'internal_staff'`) never go through Stripe checkout. `mark_order_paid(order_id, actor_id)` is the staff-only escape valve — a logged, service_role-gated way to mark an order paid for invoiced/offline/comped arrangements before dispatching it. Same auth pattern as every other Retool write.
- **`cancel_order(p_order_id, p_actor_id, p_reason default null)` (added 20260812).** The only way to move an order into `fulfilment_status = 'cancelled'` — cancellation was previously undesigned/unbuilt (per the deferred-until-credits note above). Same auth pattern as every other staff-only RPC: `service_role` only + `assert_internal_actor`, logged to `audit_log` and appended to the order's own `fulfilment_log`. Only succeeds while `fulfilment_status = 'awaiting_dispatch'` — anything already dispatched needs a real-world return, not a status flip. Sets both `fulfilment_status` and `payment_status` to `'cancelled'`; no invoice voiding or credit restoration yet (credits don't exist until Phase 8 — deliberately not built ahead). Exposed in Retool as a "Cancel order" button, same pattern as `mark_order_paid`.
- **No customer confirmation for return orders.** There is no `confirm_sent` action — a customer's own say-so that they've posted a device back isn't verifiable, so it was removed (20260811090000). Return orders stay in `dispatched` until either staff record physical receipt or a future Sendcloud tracking integration confirms it; the check-in nudge (`checkin_sent`) keeps reminding the customer every few days in the meantime, indefinitely, until that signal arrives. `confirm_received` (ship-to-new-employee orders) is unaffected — that's a real, verifiable endpoint and still works as before.
- **Settings page — admin-only, no new RPC (added 20260813).** `public.users` gained nullable `full_name` and `phone` columns; no RLS policy change was needed for either that or company details, because `users_update_admin_or_internal` and `companies_update_admin` already permit a `company_admin` to write both tables — this feature is pure CRUD UI on top of policies that already existed. Lovable `/settings` route (admin-gated via `useSession().isCompanyAdmin`, non-admins see a message instead of the forms — the RLS policy would silently no-op their writes anyway, but the UI doesn't rely on that) has three cards: company details (name/billing_email/address/vat_number — `domain`, `status`, and the Stripe id columns are deliberately not exposed here), the admin's own profile (`full_name`/`phone`, email shown read-only), and password change via `supabase.auth.updateUser({ password })` (operates on `auth.users`, no schema involvement). Settings link lives in the sidebar footer, not the main nav list, and only renders for `isCompanyAdmin`. **Known gap, not addressed:** there is still no self-update policy letting a plain `company_member` edit even their own `users` row — out of scope since this was explicitly requested as admin-only, but worth a follow-up policy if company_member self-service is ever wanted.
- **`set_company_id_from_session()` (the `employees`/`addresses` BEFORE INSERT trigger) also permits `service_role`, not just `internal_admin`/`internal_ops` (fixed 20260813, migration `20260813150000_fix_service_role_on_employees_addresses_trigger.sql`).** Found by a genuinely clean, fully-replayed local `db reset` — every prior "green suite" verification in this project's history had apparently run against an incrementally-migrated local DB, never a from-scratch replay of every migration file, so this had been silently broken since the trigger was introduced in Phase 2/3 (`20260807231000_server_side_company_id_on_insert.sql`) without ever being exercised end-to-end. Root cause: BEFORE INSERT triggers fire even for connections that bypass RLS — `service_role`'s `BYPASSRLS` only skips policy evaluation, never trigger execution — but the trigger's own gate only recognised the two *authenticated-user* internal roles; a `service_role`-keyed request carries a bare `{"role":"service_role"}` JWT with no `company_id` claim and isn't `internal_admin`/`internal_ops` either, so it fell through to the same branch an ordinary profile-less user hits and raised `'Your account is not attached to a company yet.'` on **every** service-role insert into `employees`/`addresses`, unconditionally. Never fired in production (Lovable's portal always inserts as the authenticated company user; Retool never writes straight to tables per rule 7 above) but broke the RLS suite's own `adminClient.from("employees"/"addresses").insert(...)` fixture setup — the same admin-arranges-fixture pattern used safely against every other table — which cascaded into 15 of the suite's ~30 describe blocks (everything downstream of creating a test order transitively creates a test employee first). Fix mirrors the existing `auth.role() = 'service_role'` check already used in `create_internal_order()`: `service_role` is now treated the same as `internal_admin`/`internal_ops` — no `company_id` claim to derive from either, so it must supply `company_id` explicitly, same requirement, same error if omitted. Confirmed via the trigger source, not guessed: `create_order()`'s own null-company guard raises a *different* message (`'Must belong to a company'`), which is what pinpointed this one function as the sole source of the "not attached to a company" text. Also fixed, unrelated: two RLS-suite assertion bugs found in the same clean run — `count: "exact"` passed to the wrong `.select()` overload after `.update()` (silently dropped; `count` must go on `update()`'s own options) in the `companies`/`users` update-denial tests, and a wrong error-vs-zero-rows expectation in the `kit_types`/`cover_tiers` write-deny tests (Supabase grants `UPDATE` to `authenticated` by default — RLS, not the SQL grant, is what blocks it, so a zero-policy table's UPDATE succeeds with zero rows affected rather than erroring).
- **`create_order()`/`create_internal_order()` had five/four coexisting overloads, causing PostgREST `PGRST203` "Could not choose the best candidate function" (fixed 20260813, migration `20260813160000_drop_obsolete_create_order_overloads.sql`).** Surfaced immediately after the `service_role` trigger fix above finally let the order-creation describe blocks run at all. Root cause: `create or replace function` only replaces a function when the new definition has the *exact same parameter list* — every migration that added a new optional param to `create_order`/`create_internal_order` (`enhanced_cover_schema_and_rpcs.sql` → `credits_schema_and_rpcs.sql` → `notify_employee_order_flag.sql` → `manual_employee_entry_on_order.sql`) silently created a new overload alongside the previous one instead of replacing it, because Postgres treats a different arg list as a different function identity; none of those migrations ever dropped the version they were superseding. Confirmed directly against `pg_proc`/`pg_get_function_identity_arguments` on the hosted project before fixing: five live `create_order` overloads (9/10/11/12/19 params) and four live `create_internal_order` overloads (11/12/13/21 params). PostgREST resolves an RPC call by matching the request body's named keys against candidate signatures; a caller supplying only the older common param subset (every newer param has a default) matches multiple overloads equally well, so PostgREST refuses to guess. This is exactly the kind of thing `CREATE OR REPLACE FUNCTION`'s Postgres semantics trap people with — worth remembering for *any* future RPC that grows new optional params: drop the old signature explicitly in the same migration, don't rely on `create or replace` alone. Fix: a migration that `drop function if exists`s each obsolete historical signature by its exact old arg list, leaving only the current 19-param `create_order` and 21-param `create_internal_order`. Verified empirically post-fix: `select proname, count(*) from pg_proc ... group by proname having count(*) > 1` returns zero rows across the entire `public` schema — this was the only function pair with a duplicate-overload problem, nothing else needs the same treatment.
- **SEVERE, since-fixed: six `SECURITY DEFINER` functions had leaked `EXECUTE` to `anon`/`authenticated` on the live database (found and fixed 20260813, migration `20260813170000_lock_down_leaked_internal_function_grants.sql`).** Same root cause documented once already for `record_stripe_payment` (`20260807230100_lock_down_record_stripe_payment.sql`): Supabase grants `EXECUTE` on every new `public`-schema function to `anon`/`authenticated` by default, and `revoke all ... from public` does **not** remove that grant — only an explicit `revoke execute on function ... from anon, authenticated` does. That lesson went unheeded four more times as new functions shipped. Worst finding: `get_sendcloud_webhook_secret()` had **no internal-role check in its body at all** and returned the plaintext Sendcloud webhook signing secret to any caller holding just the public `anon` key — live-exploitable to forge signed Sendcloud webhook payloads against a project that went live with real Stripe payments the day before. Also leaked, lower severity: `record_credit_purchase()` and `record_card_setup()` (no internal check either — arbitrary invoice/credit minting and arbitrary `stripe_payment_method_id` overwrite were both possible), plus `apply_sendcloud_tracking_event()`, `create_internal_order()`, and `assert_internal_actor()` (all three already had a `service_role`-only guard in the function body, so not actually exploitable, but locked down for defense-in-depth consistency with the rest of the schema). Found by a direct `pg_proc`/`information_schema.routine_privileges` query against hosted, not guessed. Fixed immediately via `apply_migration` straight against the hosted project (this one didn't wait for the usual local-first workflow, given live exploitability), then backfilled into this local migration file. Also added: `public.internal_function_grant_leaks`, a `public`-schema view re-exposing the relevant slice of `information_schema.routine_privileges` (PostgREST/`supabase-js` can't query `information_schema` directly, only `public`-schema tables/views), read-only to `service_role`, with a permanent regression test in `tests/rls.test.ts` asserting zero leaked grants across every internal-only function by name — this exact bug class had recurred five times silently; it's now a standing check instead of something that only gets caught by an ad-hoc query someone happens to think to run.
- **Credit redemption's `p_pay_with_credit` path was 100% broken since the feature shipped on 20260812 — every credit-paid order failed `orders_credit_snapshot_consistent` (fixed 20260813, migration `20260813180000_fix_credit_redemption_check_constraint_violation.sql`).** Root cause: Postgres `CHECK` constraints are never deferrable (unlike `PRIMARY KEY`/`UNIQUE`/`FOREIGN KEY`), so they're evaluated immediately at the end of the statement that touches the row — but `create_order`/`create_internal_order` inserted the `orders` row first (with `paid_with_credit = true` and `credit_transaction_id` defaulting `NULL`), then inserted the `credit_ledger` debit row, then `UPDATE`d `orders.credit_transaction_id` afterward. The very first `INSERT` always violated `check (paid_with_credit = (credit_transaction_id is not null))`, unconditionally, on every call. This had apparently never been caught because the RLS suite itself never got far enough to exercise this path until the `service_role` trigger and overload-ambiguity bugs above were fixed — the credits feature had been documented in this file as "verified working" based on the code review, not an actual successful production redemption. Fix: reorder so the `credit_ledger` debit row is inserted *first* (with `order_id` left null — nullable FK, no consistency constraint on that side), then the `orders` row is inserted once with `credit_transaction_id` bound directly in the same statement, then `credit_ledger.order_id` is backfilled after. Applied directly to hosted via `apply_migration` and confirmed via `pg_get_functiondef` before writing the local migration file, to make sure the reordering started from the actual live function body, not a stale local copy.
- **"Check Tracking Now" — a manual, staff-triggered poll fallback for Sendcloud tracking (added 20260813, migration `20260813200000_sendcloud_poll_fallback.sql`, extended `20260813210000_extend_sendcloud_poll_status_map_from_real_payload.sql`).** Modelled on (not copied from) Base44's old `pollSendcloudTracking` button — a genuine gap, not a nice-to-have: while going back through Phase 6 to confirm the webhook integration against a real live order, `sendcloud_webhook_events` turned out to sit at **zero rows** — no Sendcloud webhook has ever actually reached this system, for any order, despite `sendcloud-webhook` being deployed and active. Not proof the push path is broken, but it meant there was no way to know an order's real delivery status if a webhook never arrives. This adds a pull-based path that works independently of whatever's going on with the push side, per the user's own recommendation ("keep a sync function as a backup"). Deliberately a **separate code path** from `apply_sendcloud_tracking_event()`, not a shared one: confirmed directly from source, Sendcloud's webhook payload (`events[].status_code`, matched against `sendcloud_status_map`) and its v2 tracking-poll endpoint (`statuses[].parent_status`, e.g. `announcing`, `shipment-on-route`, `delivered`) are genuinely different API surfaces with different vocabularies — not a naming variant of the same thing. New table `sendcloud_poll_status_map` (parent_status → normalized_status) keeps the two vocabularies from ever being cross-matched. New RPC `apply_sendcloud_poll_result(p_order_id, p_actor_id, p_tracking_number, p_carrier_code, p_parent_status, p_status_description, p_event_at)` mirrors `apply_sendcloud_tracking_event()`'s transition rules exactly (`dispatched → in_transit`, `{dispatched,in_transit} → completed`, same `ship_to_new_employee` `confirmed_received_at` stamping) but is looked up by `order_id` directly (the caller already knows which order it's polling, unlike the webhook's tracking-number scan) and is **actor-attributed** — `actor_id` is always `p_actor_id`, never null, so a `fulfilment_log` entry from a staff-triggered poll is distinguishable from the webhook path's system-triggered (null-actor) entries; each poll log entry also carries `detail.source: 'poll'` for the same reason. New RPC `get_sendcloud_api_credentials()` returns both the public key and secret key for Basic Auth (the existing `get_sendcloud_webhook_secret()` only ever returned the secret key alone, scoped to HMAC verification) — both new functions got an in-body `auth.role() = 'service_role'` check **and** an explicit `revoke execute ... from anon, authenticated` from the start, applying the lesson from the same day's security-lockdown incident rather than repeating it a sixth time. New Edge Function `poll-sendcloud-tracking`: since Postgres can't make outbound HTTP calls without `pg_net` (which this project's shipped code doesn't use — every other external call is an Edge Function), it reproduces Retool's existing trust boundary in Deno instead of a plain RPC — `verify_jwt: false`, and the function checks the `Authorization` bearer against `SUPABASE_SERVICE_ROLE_KEY` itself, the same threshold every other Retool-triggered RPC enforces via `auth.role()`, just one layer up. Polls Sendcloud's **v2** `GET /tracking/{tracking_number}` (not v3): v2 is in Sendcloud's "maintenance mode" but explicitly documented as remaining fully functional for existing integrations, and the only v3 parcel-tracking surface found was a beta POST endpoint for *registering* external parcels, not a GET-by-tracking-number status lookup — the more conservative, already-documented choice for a fallback whose entire point is reliability. **Live-tested 2026-08-13 before going anywhere near Retool**, using `pg_net` directly against Sendcloud's real API with a genuine parcel (the user assigned a real, already-delivered Royal Mail tracking number, `MZ531042790GB`, to test order `RKM-260809-001`) — this caught a real bug pre-ship: the response's `statuses[]` array is **not newest-first** in practice (a real payload showed index 0 was `no-label` from days earlier, `delivered` entries were last), the opposite of what the published OpenAPI example ordering implied; fixed to select by max `carrier_update_timestamp` instead of trusting array position, redeployed, then re-verified against the same real payload via the RPC directly (transition applied, idempotent on replay, `actor_id` and `source: 'poll'` both present as designed). Three previously-unseeded `parent_status` values turned up in that same real payload and were added: `no-label` and `announced` → `ignored` (both pre-pickup, no `fulfilment_status` slot for either), `driver-on-route` → `in_transit` (final-mile attempt). **Known side effect:** that live test genuinely completed `RKM-260809-001` in the hosted database (correct behaviour, not a bug — the test parcel really was delivered) — its `fulfilment_log` now carries a real `poll`-sourced `delivered` entry. **Retool UI wired and published 20260814.** A new backend mutation, `/backend/mutations/checkTrackingNow.ts` in the "ReturnKits Ops" Retool React app, calls `poll-sendcloud-tracking` via the existing "Supabase REST" resource (same resource every other RPC mutation already uses for its service_role key, just pointed at `/functions/v1/poll-sendcloud-tracking` instead of a `/rest/v1/rpc/...` path — confirmed this resource's base URL isn't scoped to `/rest/v1` only, by reading the resource and the existing mutation files directly rather than assuming). Throws only on a top-level `error` field in the response, same `SupabaseErrorBody`-style check as `markOrderPaid.ts`, otherwise returns the full parsed body so the frontend can show per-tracking-number results. `Orders.tsx` gained a "Check tracking now" button (`RefreshCw` icon from `lucide-react`) in the same action-button row as "Mark as paid"/"Edit tracking"/"Cancel order", disabled unless the order has at least one tracking number and isn't already `completed`/`cancelled`; on success it toasts a concise per-tracking-number summary (`tracking_number → new_status`, or the `reason`/`error` if nothing changed) and refetches the orders table. Built by prompting the Retool React-app AI builder directly via MCP (`retool_create_or_append_react_app_thread_message`) rather than editing files by hand — Retool React apps have no direct file-write tool, only this thread-based mechanism — then verified by reading both changed files back and diffing them against the intended pattern before approving the one pending mutating-function review (`retool_list_pending_react_app_function_approvals` flagged the new `supabaseRest` call, as expected for any new mutation) and publishing (`https://returnkits--returnkits-ops.retool.app`). Deliberately started as a fresh thread from published main rather than continuing an older unrelated in-progress thread ("Add Postgres Query and Table Component", ~23h stale, never published) sitting on the app, to avoid entangling this change with unknown unmerged work. **Still not addressed:** the Edge Function's own bearer-check layer has not yet been exercised via a genuine live click in the published app — the RPC layer (`apply_sendcloud_poll_result`) was live-tested directly via `pg_net` on 20260813, and the mutation file's shape was verified by direct code review, but nobody has clicked the actual button against a real order yet. Worth a real click on a dispatched order with a live tracking number as the first real-world check.
- **Communication log surfaced on both sides (added 20260814).** Prompted by a user question about whether notifications had stopped for an order after Check Tracking Now auto-completed it — answering that meant hand-querying `communication_log` directly, which flagged this as something both customers and staff should be able to see themselves. No schema or RLS changes: `communication_log` and its RLS (`communication_log_select_own_company`, proven in `tests/rls.test.ts`) already existed from Phase 5/the employee-notifications work, this is purely two new read surfaces. **Lovable (customer-facing):** `orders.$id.tsx` gained a second `useQuery` (`["order-comms", id]`) reading `communication_log` filtered to `.eq("order_id", id).eq("audience", "customer")` — that audience filter is load-bearing, not decorative: the same table also holds `internal` (staff-only) and `employee` (recipient-nudge, not this logged-in user) rows for the same order, and RLS only scopes by company, not audience, so the client query is what keeps those out of the customer's view. Rendered as a new "Updates" card (`lg:col-span-3`, same grid as the other order cards) mapping the DB's CHECK-constrained `type` values to friendly labels (`order_confirmation` → "Order confirmed", `dispatched` → "Kit dispatched", `checkin_sent` → "Return reminder", `checkin_received` → "Delivery check-in") and collapsing the six-value `status` CHECK domain down to just "Delivered" / "Sent" / "Not delivered" — raw internal delivery-failure vocabulary (`bounced`/`complained`/`failed`/`suppressed`) is deliberately never shown verbatim to the customer. **Retool (staff-facing):** new read-only backend query `/backend/queries/getCommunicationLog.ts`, same `supabasePostgres.query()` direct-read pattern as `getOrders.ts`/`getInternalStaff.ts` (reads may hit Postgres directly per the existing Retool convention; only writes go through RPCs/Edge Functions), returning **all** audiences unfiltered — this is the ops view, staff need to see the `internal` and `employee` rows too. Wired into `Orders.tsx` via a `useEffect` on `selectedOrderId` (fetches fresh each time a different order is selected) and rendered as a small table at the bottom of the existing order detail panel: type, an audience badge (customer/employee/internal, colour-coded), recipient, a status badge (raw status shown here, unlike Lovable, since this is staff), and timestamp; loading skeleton and "No communications logged yet." empty state match the rest of the panel's existing patterns. Both changes built the same way as Check Tracking Now: Lovable via `send_message` against project `7685dcd8-32c2-4dae-b672-93de0c1b4caa`, Retool via a fresh AI-builder thread (`retool_create_or_append_react_app_thread_message`, `startNewThreadFromMain: true`) then published; both verified by reading the changed files back before considering it done. No pending mutating-function review was raised for the Retool side (unlike `checkTrackingNow.ts`) since this is a plain read, not a write.
- **Credit redemption's advertised race-safety was never actually implemented (fixed 20260813, migration `20260813190000_serialize_concurrent_credit_redemption.sql`).** `20260812220000_credits_schema_and_rpcs.sql`'s own comment on `create_order` claimed "two concurrent redemptions racing for the last credit will serialize, not double-spend" — but the function never enforced that; the balance is a `SUM` over `credit_ledger` read with no lock, so under Postgres's default READ COMMITTED isolation two concurrent `p_pay_with_credit=true` calls for the same company+kit type could both read the same balance, both pass the `>= 1` check, and both insert a debit row — a real double-spend, exactly the `claimFreeKit` bug class this project's own Base44 audit flagged. This was invisible until now because the `orders_credit_snapshot_consistent` bug above made *every* credit-paid call fail regardless of timing, so the RLS suite's "two concurrent redemptions against a balance of one" test was getting 0 successes for the wrong reason (both calls hit the constraint violation) rather than confirming real serialization. With that bug fixed, re-running the test surfaced the actual gap. Fix: `pg_advisory_xact_lock(hashtextextended(company_id || ':' || kit_type_id, 0))` taken immediately before the balance read whenever `p_pay_with_credit` is true — transaction-scoped (auto-released on commit/rollback), and scoped to the specific company+kit-type pair so unrelated redemptions never block each other. The second concurrent caller now blocks until the first transaction commits, then re-reads the now-debited balance and correctly fails with "insufficient balance" instead of double-spending.
- **"Your return is on its way back" — a new customer email fired off the courier's first scan on the return leg (added 20260814, migration `20260814150000_add_return_in_transit_comms_type.sql`).** Born from a brainstorm about maximum-visibility return tracking as a selling point, deliberately scoped down through back-and-forth with the user to one concrete trigger: a **return-service order's** `fulfilment_status` transitioning `dispatched → in_transit` — the point at which the leaver has physically handed the box to a courier. Recipient is **only the ordering user** (`orders.created_by`), never the leaver/employee — confirmed explicitly with the user, and consistent with the existing "employees never log in, can't self-report" rule that already removed `confirm_sent` once for the same underlying reason. **Ship-to-new-employee orders' outbound leg is explicitly excluded** — return orders only, per the user's direct answer. New `communication_log`/`notification_preferences` type `return_in_transit`, added by widening both CHECK constraints (both are separate, independently-defined constraints, not a shared one — confirmed via `pg_get_constraintdef` before writing the migration, not assumed). Hooks into the **existing** `dispatched → in_transit` transition rather than inventing new tracking logic: both `apply_sendcloud_tracking_event()` (webhook path) and `apply_sendcloud_poll_result()` (poll path) already report `{applied: true, new_status: 'in_transit', leg: 'return', order_id}` on exactly this transition, and `leg` only ever comes back `'return'` when the tracking number matched `return_tracking_number` — which ship-to-new-employee orders never have, so the trigger condition can't misfire onto the wrong service type by construction (this exact guarantee is what `tests/rls.test.ts`'s pre-existing "return leg" assertions on both RPCs already prove, reused rather than re-tested). Both Edge Functions (`sendcloud-webhook`, `poll-sendcloud-tracking`) now call `send-order-email` with `type: "return_in_transit"` immediately after a matching apply result, non-blocking (a failure is Sentry-captured but never fails the webhook's 200 ack or the poll's own result payload back to Retool) — same `fetch(`${SUPABASE_URL}/functions/v1/send-order-email`)` pattern `send-checkin-notifications` already established. Content includes an estimated arrival date: **confirmed present** as a top-level `expected_delivery_date` field on a real Sendcloud v2 tracking-poll response (live-tested 20260814 against the same real tracking number used in the Check Tracking Now live test) — worth noting the real value was a day *early* against the parcel's actual delivery, which is why the email copy reads "around {date}, courier estimates can shift by a day or so" rather than a bare promise. `poll-sendcloud-tracking` passes this straight through from its own already-parsed response; `sendcloud-webhook` reads the same field defensively from the webhook payload (Sendcloud's docs say the webhook payload shape matches the poll response, but this specifically has **not** been empirically confirmed, since zero real webhooks have ever been received end-to-end — see the poll-fallback migration's own note) and falls back to `add_working_days(today, 2)` — the existing SQL helper, not new date logic — when absent. `send-order-email` gained the `return_in_transit` type end to end: new template function (reuses the existing `field()`/`trackButton()`/`layout()` helpers, nothing new invented), an optional `estimatedArrivalDate` request-body param, the `orders` query extended to also select `return_courier`/`return_tracking_number`/`return_tracking_url`, and the same one-shot idempotency re-check (`communication_log` status in `sent`/`delivered`) already applied to `order_confirmation`/`dispatched`. `sendEmployeeCopy()` needed no changes — its existing type-guard (`dispatched`/`checkin_sent` only) already excludes this type, consistent with "customer-only, no employee copy" being correct by construction rather than an added exclusion. **Gap found and fixed in the same pass:** `seed_default_notification_preferences()` (the trigger that seeds a company's mute-toggle rows) hardcodes its own list of 4 event types and was never extended — migration `20260814160000_seed_return_in_transit_notification_preference.sql` adds the 5th type to the trigger and backfills every existing company. Without this fix the email would still have sent correctly (`notification_enabled()` defaults `true` for an event type with no row, proven in `tests/rls.test.ts`), but no company admin would ever see a toggle for it in Settings — an oversight, not a scope decision, caught while writing tests for this feature rather than assumed fixed by the type-CHECK migration alone. `sendcloud-webhook`'s Sentry reporter was also inlined in this same pass (previously importing from `../_shared/sentry.ts`) — it predated the `_shared` MCP-deploy bundling issue already fixed for three other functions and had never been proven to survive an MCP-based redeploy since that issue was diagnosed, so it was fixed pre-emptively rather than risked on this deploy. **Closed the same day:** this email now also fires off the new automatic hourly poll — see the next bullet — closing the gap where it previously only sent off a genuine Sendcloud webhook (still zero, ever, in production) or a manual "Check Tracking Now" click.
- **Automatic hourly Sendcloud tracking polling — no manual click needed (added 20260814, migration `20260814170000_scheduled_sendcloud_tracking_poll.sql`).** Direct follow-on from the bullet above: without this, `return_in_transit` and Phase 6 tracking generally only ever updated off a genuine webhook (zero, ever) or a staff member's manual click. Frequency confirmed directly with the user after asking about resource/rate-limit risk: Sendcloud's own published GET rate limit is 1000 req/min (`sendcloud.dev/docs/getting-started/rate-limits.md`), nowhere near this project's order volume — user chose **hourly**. Design problem this migration solves: `apply_sendcloud_poll_result()` was built exclusively for the human-triggered "Check Tracking Now" flow and mandated a real internal-staff `p_actor_id` (`assert_internal_actor` raises otherwise) — a pg_cron job has no human behind it. Considered and rejected: a fake "system" staff user, blocked by `public.users.id`'s hard FK to `auth.users(id)` and misleading in the audit trail. Chosen instead: `p_actor_id` now accepts `null` and is treated as system-triggered — the exact convention `apply_sendcloud_tracking_event()`'s webhook path already used, not a new one — validated only when non-null (`if p_actor_id is not null then perform assert_internal_actor(...)`), with each `fulfilment_log`/`audit_log` entry's `detail.source` distinguishing `'webhook'` (implicit, always null actor), `'poll'` (human, real actor_id), and the new `'scheduled_poll'` (pg_cron, null actor) — so "why did this order's status change" is always answerable from the log alone. New plain-SQL helper `orders_needing_tracking_poll()` (same no-`SECURITY DEFINER`, RLS-inherits-from-caller shape as `orders_needing_checkin()`, EXECUTE explicitly revoked from `public`/`authenticated` to match its grants exactly — confirmed via `information_schema.routine_privileges`, not assumed, after an initial migration draft left `authenticated`/`PUBLIC` grants in place and had to be corrected) returns every order `dispatched`/`in_transit` with at least one tracking number. New wrapper `trigger_scheduled_tracking_poll()` mirrors `trigger_checkin_notifications()`'s exact pattern (Vault-sourced `service_role_key`, `net.http_post` to the Edge Function, fire-and-forget) — deliberately copied, not reinvented. `cron.schedule('sendcloud-tracking-poll-hourly', '5 * * * *', ...)` — 5 minutes past the hour, not on it, purely to avoid landing in the same second as the existing `checkin-notifications-hourly` job (`0 * * * *`); no real contention either way since both are async via `pg_net`, just tidiness. `poll-sendcloud-tracking` now serves two callers behind the same auth boundary: a request body with `order_id` (+ real `actor_id`) is the existing manual Retool path, unchanged; a body **without** `order_id` is the new scheduled path — calls `orders_needing_tracking_poll()`, polls every tracking number on every eligible order via a newly-extracted `pollOneTrackingNumber()` helper (shared by both paths so the Sendcloud-calling/result-parsing logic exists exactly once), `actorId: null` throughout. **Live-tested 20260816** via `select public.trigger_scheduled_tracking_poll();` directly (exactly how the cron job invokes it, no raw service-role key ever needed in-session) — real HTTP 200, `orders_polled: 3`, correct per-order/per-tracking-number enumeration, graceful 404 handling for the (test/placeholder) tracking numbers on those three orders. Also directly verified against hosted: the null-actor accept path (`RKM-260809-002`'s real `dispatched → in_transit` transition, `actor_id: null`, `detail.source: 'scheduled_poll'` in the resulting `fulfilment_log` entry) and that a non-null-but-invalid `actor_id` is still rejected — the null exception is not a blanket bypass. **Known side effect:** that same live verification genuinely transitioned `RKM-260809-002` to `in_transit` in the hosted database using a synthetic test event (real behaviour, not a bug — same category of side effect as the `RKM-260809-001` live poll test on 20260813).
- **`checkin_sent` customer email now says whether (and why not) the employee is also being nudged (added 20260816, `send-order-email` v21, no schema change).** Raised by the user questioning whether `orders.created_by` even needs this email, then refined collaboratively into a three-way branch rather than one generic line, because a flat "we've sent them a reminder too" would be false whenever `notify_employee` is off — which is the default. `buildCheckinSentEmail()` now takes `employeeName: string | null` and `employeeStatus: 'notified' | 'notify_off' | 'no_email'`, computed at the `checkin_sent` call site in `handleRequest` from `o.notify_employee` and `resolvedEmployee.email` (the existing manual-entry/directory resolver, unchanged): `notified` → "We've also sent {Employee} a reminder"; `notify_off` → "You may want to follow up with {Employee} directly — employee notifications weren't turned on for this order"; `no_email` → "You may want to follow up with {Employee} directly — we don't have an email on file for them." The two "not notified" branches are deliberately distinct, not collapsed into one fallback: `notify_off` is the orderer's own choice at order creation (nothing to fix, they can just follow up themselves), `no_email` is a real, fixable data gap (add the employee's email to the directory) — telling the orderer *why* is more useful than a generic "we didn't notify them." Reflects configuration, not delivery confirmation — mirrors `sendEmployeeCopy()`'s own eligibility check (`notify_employee` + employee has an email) but doesn't wait for that send to actually succeed, consistent with how this codebase never retroactively reconciles customer-facing copy against downstream delivery outcomes anywhere else. Pure Edge Function template logic, no new RPC/constraint/table, so no `tests/rls.test.ts` changes — deployed and verified via `list_edge_functions` (version 21) only.

Phases 0–5 are the launch build (~5–7 weeks); everything after is demand-driven. **Do not build ahead.** Deferred at launch: bulk CSV ordering, inventory, free-kit promo, KB, MDM, BambooHR, escalation, HubSpot sync, API keys, SMS. (Enhanced Cover and prepaid credits were both pulled forward on 20260812 — see the locked decisions above — everything else in this list is still genuinely not built. Promo credits specifically are still not built — only the prepaid-purchase half of the credits design shipped.)

Labels are still bought **manually in Sendcloud's dashboard** — Phase 6 only added tracking webhooks, not label purchase automation. Deliberate — two minutes per order at launch volume, zero build time for that part.

Current phase: **6 (tracking-only) — Sendcloud webhook integration**. Phases 0–5 (foundations through notifications) are done and verified live.

## Gotchas from the prototype

- Base44 declared enums but didn't enforce them — 51 of 54 orders held a status the schema prohibited. Use `CHECK` constraints and Zod at every boundary.
- No foreign keys meant 9 of 11 companies referenced users that didn't exist. Add FKs on every relation.
- No transactions meant the free-kit claim marked itself claimed *before* granting the credit. In Postgres, use one transaction with a conditional `UPDATE ... WHERE claimed = false RETURNING`.
- Two companies both claimed `gmail.com` as their domain. This is why domain-based joining was removed entirely.

## Environment

Secrets in Supabase Vault, never plain DB columns or committed config. Supabase project region must be **London (eu-west-2)**.

**Stripe went live 20260812.** `STRIPE_SECRET_KEY` (Edge Function secret) was cut over to a live key. Sendcloud's live/test status wasn't part of this cutover and hasn't been separately re-confirmed — check before assuming it's live too. Stripe scopes Customer objects to test/live separately, so the first live checkout failed ("a similar object exists in test mode, but a live mode key was used") — Sentry caught it, fixed by migration `20260812180000_clear_stale_test_mode_stripe_customer_ids.sql` nulling every cached `companies.stripe_customer_id` so `create-checkout-session`'s existing lazy-create path re-created them against the live key. Verified working end-to-end same day: invoice #5 (`cs_live_...`, £48.00 inc VAT) went through the full live pipeline — checkout → Stripe → live webhook → signature verified → `record_stripe_payment` → invoice issued. That order was later cancelled via `cancel_order`; per the no-automated-refunds rule above, any refund on it has to be issued manually in the Stripe dashboard — `cancel_order` never touched Stripe.
